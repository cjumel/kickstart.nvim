-- nvim-notify
--
-- A fancy, configurable, notification manager for NeoVim.

return {
  "rcarriga/nvim-notify",
  lazy = true,
  opts = {
    background_colour = "#000000", -- Disable warning with transparent background
  },
}
