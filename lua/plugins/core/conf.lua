-- conf.nvim
--
-- A tiny (tiny) plugin for simple machine-, project- and command-level configuration for Neovim using Lua.

return {
  "cjumel/conf.nvim",
  lazy = true, -- Can be directly required by other plugins
}
