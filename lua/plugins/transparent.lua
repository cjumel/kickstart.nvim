-- transparent.nvim
--
-- Remove all background colors to make nvim transparent. Call `TansparentToggle` to enable/disable.

return {
  "xiyaowong/transparent.nvim",
  priority = 1000, -- UI stuff should be loaded first
}
